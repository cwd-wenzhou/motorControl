create database motor precision "ns";

CREATE TABLE `sample_0`
(`start_time` TIMESTAMP,
 `end_time` TIMESTAMP,
 `sample_name` NCHAR(10)
 );

CREATE TABLE `sample_1`
(`start_time` TIMESTAMP,
 `end_time` TIMESTAMP,
 `sample_name` NCHAR(10)
);